<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="500"
             d:DesignHeight="300"
             x:Class="ATC4_HQ.Views.OpenALInstallView">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="OPENAL 安装"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- 说明文字 -->
        <TextBlock Grid.Row="1"
                   Text="检测到系统未安装 OPENAL 库，这是运行游戏所必需的组件。"
                   FontSize="16"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- 自动安装说明 -->
        <StackPanel Grid.Row="2"
                    Margin="0,20,0,20">
            <TextBlock Text="安装：" 
                       FontSize="16" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,10"/>
            <TextBlock Text="1. 点击下方按钮自动下载安装包" 
                       FontSize="14" 
                       Margin="0,0,0,5"/>
            <TextBlock Text="2. 程序将自动解压并运行安装程序" 
                       FontSize="14" 
                       Margin="0,0,0,5"/>
            <TextBlock Text="3. 按照安装向导完成安装" 
                       FontSize="14" 
                       Margin="0,0,0,10"/>
            <Button Name="OpenWebsiteButton"
                    Content="下载并安装"
                    Width="150"
                    Height="35"
                    FontSize="14"/>
        </StackPanel>

        <!-- 测试安装按钮 -->
        <Button Grid.Row="3"
                Name="TestInstallButton"
                Content="测试安装"
                HorizontalAlignment="Center"
                Width="100"
                Height="35"
                FontSize="14"
                Margin="0,0,0,10"/>

        <!-- 返回按钮 -->
        <Button Grid.Row="4"
                Name="BackButton"
                Content="返回"
                HorizontalAlignment="Center"
                Width="100"
                Height="35"
                FontSize="14"/>
    </Grid>
</UserControl>
